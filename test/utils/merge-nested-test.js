import mergeNested from 'validated-changeset/utils/merge-nested';

describe('Unit | Utility | merge nested', function() {
  // Replace this with your real tests.
  it('it works', async function(assert) {
    const result = mergeNested();
    assert.ok(result);
  });
});
